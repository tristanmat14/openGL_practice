# LearnOpenGL practice code
A repository for all the code in the [learnOpenGL](https://learnopengl.com/)

# Setup
1) Install the mingw cross compiler and relavent libraries via the command:
`sudo apt install mingw-w64`

